# ML and Computer Vision dependencies
# Note: torch and torchvision are installed separately in Dockerfile with CPU-only wheels
# to avoid CUDA bloat and pip backtracking loops

# DO NOT include torch/torchvision here - they're installed in Dockerfile Step 2
# torch>=2.2.0
# torchvision>=0.15.2

# Pinned versions to prevent pip backtracking and memory exhaustion
numpy==1.24.3
scikit-learn==1.3.2
mediapipe==0.10.14
opencv-python==4.8.1.78
pillow==10.1.0
colormath==3.0.0
skin-tone-classifier>=1.2.6
